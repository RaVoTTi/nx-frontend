import { Actions, createEffect, ofType } from '@ngrx/effects';
import { create } from 'domain';

// export class BooksEffects {
//   loadBooks$ = createEffect(() => this.actions$.pipe(ofType()  ));
//   constructor(private actions$: Actions) {}
// }
